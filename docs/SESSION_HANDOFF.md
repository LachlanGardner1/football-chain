# Session Handoff — Read This First After a Context Reset

This document summarizes a long working session on football-chain so a future session can continue without re-deriving context. Written 2026-08-11, updated same day after two more bodies of work (data import + solver fix, see sections 8-9). **Everything described in this document is now committed** — `git status` is clean. The first 9 commits from this session (through the streak fix) were pushed to `origin/main` on request; the last 2 (data import + solver fix, sections 8-9) are committed locally but **not yet pushed** — push them if/when asked.

## What was done, in order

### 1. Initial codebase review (no code changes)
Reviewed the whole app (gameplay logic, backend architecture, infra). Findings covered in later sections as they were acted on. Also reviewed `docs/PHASE_2_BACKLOG.md` and found its checkboxes badly out of date — several stories (scoring, local persistence, backend stats tables/API) were shown 0% done but were already fully built.

### 2. UI redesign
Full visual overhaul of `src/app/page.tsx`: dark/light theme system (CSS custom properties, toggle defaults to dark, persisted via `THEME_STORAGE_KEY` in `src/app/theme.ts`), Phosphor icons, Space Grotesk/JetBrains Mono fonts via `next/font/google` in `src/app/layout.tsx`, redesigned chain-trail/anchor-chip visuals. No functional/logic changes in this pass, styling only.

### 3. Session/auth system
Added `src/backend/services/auth/session.service.ts` — signed HMAC-SHA256 cookie-based anonymous sessions (`SESSION_SECRET` env var, falls back to an insecure dev value outside production). Secured `POST /api/complete` and `GET /api/me/stats` (previously trusted a client-supplied `userId`/`x-user-id` header — a spoofing hole). Wired the frontend to actually call these endpoints, which were previously orphaned (the game only tracked streaks client-side in `localStorage` — see section 6, that system was later removed).

### 4. AWS deployment strategy review + Lambda migration (Terraform, unapplied)
- Reviewed the original ECS Fargate + ALB + CloudFront Terraform (`infra/`). Found it incomplete (no Dockerfile anywhere, no CI/CD pipeline) and cost-mismatched for a small/low-traffic app (~$150-220/month in fixed ALB+NAT+Fargate cost regardless of traffic).
- Decided: migrate to **Lambda (via OpenNext) + CloudFront + S3**, keep RDS Postgres exactly as-is (already correctly sized), drop the in-memory graph cache in favor of direct-to-Postgres queries per request (this also incidentally fixes a correctness bug: multiple ECS tasks could previously disagree on graph state).
- **App code changes** (already live, not just infra): new `src/backend/services/graph/pg-graph.service.ts` (`PgGraphService`) replaces the deleted `InMemoryGraphService`/`GraphBootstrapService`. `GraphService` interface (`src/backend/services/graph/graph-types.ts`) is now async. `ChainValidationService` updated accordingly. `ensureServicesReady()` bootstrap pattern removed entirely from `src/backend/wiring/container.ts` and all API routes. `pg.Pool` max reduced from 10 to 3 in `src/backend/repositories/postgres/db.ts` (Lambda concurrency = many small pools, not one big one).
- **Infra changes** (Terraform written, never applied): new `infra/modules/lambda_app/`, `infra/modules/cloudfront_lambda/`. Deleted `infra/modules/alb/`, `ecs_service/`, `ecr/`, `cloudfront_alb/`. Updated `security/`, `scheduler/` (now targets a placeholder Lambda instead of `ecs:RunTask` — no real daily-publish logic exists, see section 5), `observability/` (added real CloudWatch alarms that didn't exist before), `network/` (NAT Gateway now disabled by default). Rewired `infra/environments/dev/` and `infra/environments/prod/`. Updated `infra/TERRAFORM_CHECKLIST.md`.
- Did a real local OpenNext build spike (`npx open-next build`) to verify the actual Lambda artifact shape before writing Terraform against it — found and fixed a real secret-leak bug in the process: Next.js's standalone build copies the local `.env` into the build output, and OpenNext then bundles that into the deployable Lambda zip. Fixed via `open-next.config.ts`'s `buildCommand` override to strip it post-build. `open-next.config.ts` also disables the ISR/incremental-cache machinery (SQS/DynamoDB) since this app has zero ISR usage — keeps the deployed topology to just Lambda + CloudFront + S3.
- **Nothing has been deployed.** No `terraform init`/`plan`/`apply` has been run against real AWS. The image-optimization Lambda function (unused — app has no `next/image` usage) fails to build on Windows locally (OpenNext itself warns it's not fully Windows-compatible); this doesn't block anything since that function isn't deployed anyway.

### 5. Daily puzzle / game design review (no code changes, findings acted on in section 6)
Found real bugs in the puzzle logic: 5 seeded clubs with zero edges (dead ends in the autocomplete — deferred, not fixed, since the club dataset is intentionally incomplete pending a future open-source data integration per your direction), a silent date-fallback bug in `/api/daily` (fixed in section 7), two unsynced streak systems (fixed in section 7), and puzzle generation that never verified the "required players" were actually reachable on the graph (fixed in section 6). Full writeup copied to `docs/DAILY_PUZZLE_REVIEW.md`.

### 6. Major game-rule redesign — anchor-player puzzle model
Replaced the old fixed "start player → ordered required-sequence → target player" puzzle format with: **3-5 named "anchor" players; the player picks any of them to start on, any to end on, and visits the rest in any order** — no repeats, still alternating PLAYER→CLUB→PLAYER.
- New migration `db/migrations/006_daily_puzzle_anchor_players.sql`: new `daily_puzzle_players` join table replaces the old `start_player_id`/`target_player_id`/`required_player_ids`/`required_player_names` columns (also had to drop and recreate the `v_published_daily_puzzles` view, which depended on the dropped columns).
- New solver `scripts/puzzle-generation/solver.ts` (`findShortestAnchorChain`) — tries every start/end ordering of the anchor set with a branch-and-bound backtracking search, returns the **true shortest** connecting chain (not just the first one found, unlike the old DFS), and returns `null` if the anchor set turns out unsolvable. Has its own test file, `solver.test.ts`, with a test that specifically proves it doesn't just grab a longer decoy path.
- Updated `ChainValidationService`/`ChainValidator` interface (now takes `anchorPlayerIds: number[]`, no more `startPlayerId`/`targetPlayerId`/`requiredPlayerIds`), `DailyPuzzleDTO`/`DailyPuzzle` domain types (`anchorPlayers`/`anchorPlayerIds` replace the old start/target/required fields), `daily-puzzle.repository.ts` (new SQL joining the new table), `/api/validate-chain`, `/api/daily`.
- `scripts/seed-dev.ts` rewritten to call the new solver instead of trusting hand-typed, unverified data.
- `src/app/page.tsx`: first chain step now picks from the anchor set (not an auto-injected fixed start), unordered anchor chips with visited checkmarks replace the old numbered "required sequence," removed the synthetic start/target node injection entirely (the player's own first/last submissions now are the start/end).
- Updated `docs/CONTEXT_MEMORY.md` to describe the new rules.

### 7. Date picker restriction + streak correctness (most recent work)
- New `GET /api/daily/dates` endpoint returns the real published-date range plus the server-authoritative `today` (Postgres `CURRENT_DATE`, not client clock).
- `/api/daily?date=X` now 404s cleanly for an explicitly-requested unpublished date instead of silently substituting the latest puzzle (a "strict" mode added to `getPublishedPuzzleByDate`).
- Fixed a latent timezone bug found along the way: `puzzle.date` was coming back as a full UTC timestamp (pg driver's `DATE`→JS `Date`→ISO conversion, which can shift the calendar date depending on server timezone) — now a plain `YYYY-MM-DD` string via `to_char()` in SQL, consistently everywhere.
- **Streak fix**: `refresh_user_streak` (in `src/backend/repositories/postgres/game-result.repository.ts`) now only fires when the solved puzzle's actual `puzzle_date` equals `CURRENT_DATE` — backfilling an old puzzle updates your lifetime solve count but never touches the streak. `getUserStats` now also shows `currentStreak: 0` once the last on-time solve is more than a day stale, without needing a new solve to "notice" (matches how Wordle visually shows a broken streak the moment you've missed a day).
- **Removed the parallel client-side `localStorage` streak system** (`src/app/streakStorage.ts`, `src/app/streakCalculator.ts` — deleted, along with their test) since it had no way to implement "only counts if solved on the correct day" correctly and would have kept showing a different, wrong number next to the now-correct server one. UI consolidated to one server-backed "Streak" card + a "Solves" (lifetime) card.
- Frontend date picker: `min`/`max` bound to the real published range, client-side validation rejects out-of-range/gap dates with an inline message, shows a "past puzzle won't count toward streak" notice when viewing a non-today puzzle.
- All of the above verified live via curl + browser (not just unit tests) — including deliberately backdating a streak row in Postgres to confirm the staleness display logic.

### 8. Data expansion — imported a real player/club/transfer dataset
Replaced the tiny 16-player/19-club hand-seed with a real catalog imported from [dcaribou/transfermarkt-datasets](https://github.com/dcaribou/transfermarkt-datasets) (CC0-1.0, refreshed weekly, no auth needed).
- New `scripts/data-import/import-transfermarkt.ts` (`npm run db:import-transfermarkt`, supports `--dry-run`): downloads and gunzips `players.csv`/`clubs.csv`/`transfers.csv`/`competitions.csv` (cached under gitignored `scripts/data-import/.cache/`), parses with the new `csv-parse` devDependency, filters players to peak market value ≥€8M or ≥20 international caps, derives `player_clubs` edges from each selected player's `to_club_id` transfer history (sentinel non-club destinations like "Retired"/"Without Club" are excluded automatically since they don't resolve against `clubs.csv`), and upserts into Postgres.
- Purely **additive** into the currently-active `data_versions` row — no new dataset version, no version switching. This matters: `PgGraphService.hasEdge`/`shortestPathPlayerToPlayer` (`src/backend/services/graph/pg-graph.service.ts`) always resolve against the *active* dataset version, not a puzzle's own `dataset_version_id` — switching versions would silently change what counts as a valid move for already-published puzzles, including old ones reachable via the date picker. Staying additive avoids that footgun.
- Result: catalog grew to **6,066 players / 774 clubs / ~23.8k edges**. All 5 previously dead-end clubs (flagged in `docs/DAILY_PUZZLE_REVIEW.md`) now have real edges — 2 matched directly by name, and the import surfaced 3 duplicate club rows (hand-seeded "Ajax"/"AS Roma"/"Bayer Leverkusen" vs. the dataset's official names "Ajax Amsterdam"/"Associazione Sportiva Roma"/"Bayer 04 Leverkusen") which were found and deleted after confirming nothing referenced them.
- Extracted `normalizeName()` out of `scripts/seed-dev.ts` into shared `scripts/normalize-name.ts`, used by both scripts.
- `docs/PHASE_2_BACKLOG.md` Theme 4 and `docs/DAILY_PUZZLE_REVIEW.md` updated to reflect this is done.

### 9. Fixed findShortestAnchorChain blowing up on the larger graph
Verifying section 8's import against the real graph revealed `scripts/puzzle-generation/solver.ts`'s `findShortestAnchorChain` (used by `seed-dev.ts` to pick/verify anchor sets) took several minutes and grew past 1GB of memory before being killed, for a 3-anchor case (Haaland/De Bruyne/Salah) that should be near-instant. Root cause: its depth-first branch-and-bound tried neighbors in arbitrary order with no distance heuristic, which is fine when every club has 1-3 edges (the old hand-seed) but explodes once hub clubs (Real Madrid, Man City, ...) have 50-100+ edges each.
- **Confirmed this was isolated to offline puzzle-authoring, not live gameplay**: `PgGraphService.hasEdge` (move validation) is a single indexed lookup, and `shortestPathPlayerToPlayer` (hints) already uses bidirectional BFS — neither was affected.
- Fix: precompute unconstrained BFS hop-distances from each anchor (cheap, O(V+E) per anchor, ≤5 anchors) and use them to (a) reject anchor sets in different connected components instantly, (b) try the most promising permutation ordering first via a BFS-distance lower bound, and (c) guide the backtracking search's neighbor order toward the current target checkpoint instead of exploring blindly. Still the same exhaustive, guaranteed-shortest-path search — only how fast it converges changed. Added a 300k node-expansion budget per ordering as a safety net (warns and moves to the next ordering rather than hanging again).
- Added a regression test in `solver.test.ts`: a synthetic graph with high-degree hub clubs at a similar scale to the real failure, asserting the search completes in well under 2 seconds.
- Re-verified against the real imported graph: the Haaland/De Bruyne/Salah case that had to be killed before now solves in **22ms**.

## Current environment state

- Local Postgres 18 (native Windows service, not Docker) on `localhost:5432`, database `football_chain`, user `football_app`. Credentials already in `.env` (gitignored, not repeated here) — no setup needed, just use it.
- Migrations applied through `006_daily_puzzle_anchor_players.sql`. Catalog is now the imported transfermarkt dataset (**6,066 players / 774 clubs / ~23.8k edges**), not the old 16/19/36 hand-seed — see section 8. Re-running `npm run db:seed` re-truncates and re-seeds only the small hand-typed baseline + the 2 seed puzzles; `npm run db:import-transfermarkt` (idempotent, safe to re-run) re-layers the larger dataset back on top afterward if you ever reset the DB.
- Dev server runs via `npm run dev` (port 3000). If you hit a `ReferenceError` in the browser console after files were deleted mid-session, it's very likely a stale webpack/dev-server cache or a stale browser console-message buffer in a long-lived tab — restart the dev server and/or open a fresh browser tab before assuming it's a real bug (happened twice this session, false alarm both times).
- `npm test` — 42 tests, all passing. `npx tsc --noEmit` — clean.
- Terraform (`infra/`) describes the target Lambda architecture but has never been applied. No AWS resources exist. Would need: a real OpenNext build on Linux/WSL (not Windows) to get a clean image-optimization function build (though that function isn't actually needed/deployed since the app has no `next/image` usage), a CI/CD pipeline (doesn't exist — the original repo never had one), and `terraform init`/`plan`/`apply` against real credentials.

## Recommended next steps, roughly in priority order

1. **A real puzzle-generation/rotation pipeline** — currently only 2 solver-verified but hand-picked seed puzzles exist, drawn from a tiny hand-typed anchor list even though the catalog now has 6,066 players. No automatic daily rotation, no admin tooling to pick/publish tomorrow's anchor set from the full catalog. `findShortestAnchorChain` is now fast enough (section 9) to support this at scale. The Terraform scheduler module has a placeholder Lambda wired up (`infra/modules/scheduler/`) but it doesn't do anything yet.
2. **If/when ready to actually deploy**: build on Linux/WSL or CI (not Windows) to get a clean OpenNext artifact, stand up a minimal build/deploy script or CI pipeline, then `terraform apply` from `infra/environments/dev`.
3. **Second puzzle mode** (from the original backlog) — would need a schema change first (`daily_puzzles.puzzle_date` has a hard `UNIQUE` constraint; would need e.g. a `mode` column and a composite unique constraint).
4. Smaller deferred items: RDS Proxy (not needed at current scale, add if connection limits become an issue), custom domain/HTTPS for CloudFront, tuning the market-value/caps thresholds in `import-transfermarkt.ts` if the catalog should be bigger/smaller, "more puzzle generation options" (`docs/PHASE_2_BACKLOG.md` Theme 4, Story 4.2's one remaining open item).
