project         = "football-chain"
aws_region      = "ap-southeast-2"
github_owner    = "LachlanGardner1"
github_repo     = "football-chain"
# Numeric IDs, not names - see `gh api users/<owner> --jq .id` and
# `gh api repos/<owner>/<repo> --jq .id`.
github_owner_id = "32870817"
github_repo_id  = "1315687461"
