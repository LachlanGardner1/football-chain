# Postgres connection string for local development
DATABASE_URL=postgres://postgres:postgres@localhost:5432/football_chain

# Secret used to sign the anonymous session cookie (see src/backend/services/auth/session.service.ts).
# Required in production; falls back to an insecure dev-only value otherwise.
SESSION_SECRET=change-me-to-a-long-random-string
